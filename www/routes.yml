/:
  controller: main
  action: home

/contact:
  controller: main
  action: contact

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/forget:
  controller: user
  action: pwdforget

/mail:
  controller: user
  action: sendmail

/dashboard:
  controller: admin
  action: dashboard
  security:
    - admin
    - user