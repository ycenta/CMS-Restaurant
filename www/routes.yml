/:
  controller: Main
  action: home

/contact:
  controller: Main
  action: contact

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/forget:
  controller: user
  action: pwdforget

/Newpage:
  controller: page
  action: NewPage

/readPage:
  controller: page
  action: readPage

/list:
  controller: page
  action: displayAllPages

/dashboard:
  controller: admin
  action: dashboard
  security:
    - admin
    - user